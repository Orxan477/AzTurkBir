@using Aztobir.Business.ViewModels.Home.Faculty
@model IEnumerable<FacultyVM>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/admin/Views/Shared/_AdminLayout.cshtml";
    int num = 1;
}

<div class="col-lg-4 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Şehirler</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>№</th>
                            <th>Ad</th>
                            <th>Ayarlar</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td class="ph">@num</td>
                                <td class="ph">@item.Name</td>
                                <td style="padding: 0 0 0 17px;">
                                    <a style="padding:5px;" asp-action="Update" class="btn btn-warning" asp-route-id="@item.Id"><i class="mdi mdi-table-edit"></i></a>
                                    <a style="padding:5px;" asp-action="Delete" class="btn btn-danger delete-btn" asp-route-id="@item.Id"><i class="mdi mdi-comment-remove-outline"></i></a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section ScriptModal{
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10/dist/sweetalert2.all.min.js"></script>
    <script src="~/admin/assets/js/AlertModal.js"></script>
}

